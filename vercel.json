{
  "buildCommand": "pnpm --filter penzgidromash-replica run build",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "outputDirectory": "artifacts/penzgidromash-replica/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
