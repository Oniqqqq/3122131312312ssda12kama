{
  "buildCommand": "cd artifacts/penzgidromash-replica && pnpm run build && cd ../.. && mkdir -p dist && cp -r artifacts/penzgidromash-replica/dist/* dist/",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
