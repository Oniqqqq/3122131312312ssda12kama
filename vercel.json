{
  "buildCommand": "cd artifacts/penzgidromash-replica && pnpm install --no-frozen-lockfile && pnpm run build",
  "outputDirectory": "artifacts/penzgidromash-replica/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
