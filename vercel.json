{
  "buildCommand": "pnpm --filter penzgidromash-replica run build && cp -r artifacts/penzgidromash-replica/dist ./dist",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
